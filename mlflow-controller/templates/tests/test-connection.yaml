apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlflow-controller.fullname" . }}-test-connection"
  labels:
    {{- include "mlflow-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlflow-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
